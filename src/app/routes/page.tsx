// this is a route that will be caught and then redirected to a sub-version of this folder...

"use client"

import React from 'react';
import Redirect from '@/app/components/Views/Redirect';

const RedirectPage = () => {

    return (
        <Redirect />
    );

};

export default RedirectPage;